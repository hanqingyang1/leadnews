#kafka config
kafka.hosts=192.168.25.139:9092
kafka.group=heima.${profiles.name}.${spring.application.name}

# 单消息通道，需要以sigle结尾
kafka.topic.admin-test=${kafka.topic.admin-test}
kafka.topic.submit-article-auth=${kafka.topic.submit-article-auth}